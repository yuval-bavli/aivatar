# aivatar_app orchestrator dependencies
# Install into the repo venv:
#   pip install -r aivatar_app/requirements.txt  (Windows: .venv\Scripts\pip)

httpx>=0.27
websockets>=12.0
anthropic>=0.96
python-dotenv>=1.0

# ai_tools and sound_engine are in the repo root — no pip install needed
# (sys.path is set at runtime by orchestrator.py)
